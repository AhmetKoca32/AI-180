import 'package:flutter/material.dart';

class SkinChatScreen extends StatelessWidget {
  const SkinChatScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Skin Chat Screen'),
      ),
    );
  }
} 